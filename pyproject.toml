[project]
name = "deepswe-claude-code-eval"
version = "0.1.0"
description = "Local R2E-Gym-style evaluation harness for long-horizon coding-agent behavior, driving Claude Code headless with instruction variants"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7"]

[tool.pytest.ini_options]
testpaths = ["tests"]
